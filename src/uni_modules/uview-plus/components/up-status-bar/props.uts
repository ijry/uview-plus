import { defineMixin } from '../../libs/vue'
import defProps from './statusBar'
let crtProp = defProps['statusBar'] as UTSJSONObject

export const propsStatusBar = defineMixin({
    props: {
        bgColor: {
            type: String,
            default: crtProp['bgColor']
        }
    }
})
